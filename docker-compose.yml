version: '3.8'

services:
  buildsure-api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - DB_HOST=plm1w6.h.filess.io
      - DB_NAME=buildsure_passagesee
      - DB_USER=buildsure_passagesee
      - DB_PASSWORD=d3f8fa3070f91f1a4ba7ac150b9836994e85607b
      - DB_PORT=5433
      - SECRET_KEY=your-production-secret-key-change-this
      - GEMINI_API_KEY=your-gemini-api-key-here
      - GEMINI_MODEL=gemini-pro
      - GEMINI_TEMPERATURE=0.7
      - GEMINI_MAX_OUTPUT_TOKENS=2048
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/v1/health/simple"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
